import claim from './claim';
import claimPatient from './claimPatient';
import session from './session';
import patient from './patient';

export default {
  claim,
  claimPatient,
  patient,
  session,
};
